kind = "web"
previewPath = "/"
title = "Link in Bio"
version = "1.0.0"
id = "artifacts/link-in-bio"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20081

[services.development]
run = "pnpm --filter @workspace/link-in-bio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/link-in-bio", "run", "build" ]
serve = "static"
publicDir = "artifacts/link-in-bio/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20081"
BASE_PATH = "/"
